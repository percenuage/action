name: Main

on:
  workflow_dispatch:
  push:
    branches: [master]
    tags: [v*]

env:
  REGISTRY: europe-west1-docker.pkg.dev
  IMAGE_NAME: leequid/leequid/back
  CLUSTER_NAME: leequid
  CLUSTER_ZONE: europe-west1-c
  CLUSTER_NAMESPACE: main

jobs:
  build-and-push-image:
    name: Build and Push Docker Image
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

#      - name: Authenticate to Google Cloud
#        id: auth
#        uses: google-github-actions/auth@v2
#        with:
#          token_format: access_token
#          access_token_lifetime: 300s
#          workload_identity_provider: projects/331917731638/locations/global/workloadIdentityPools/dropps-github/providers/github
#          service_account: github@leequid.iam.gserviceaccount.com
#
#      - name: Log in to Google Artifact Registry
#        uses: docker/login-action@v3
#        with:
#          registry: ${{ env.REGISTRY }}
#          username: oauth2accesstoken
#          password: ${{ steps.auth.outputs.access_token }}

      - name: Extract metadata for Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,priority=1000,prefix={{date 'YYYY-MM-DDThh-mm'}}_,enable={{is_default_branch}}
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern=v{{version}}
            type=semver,pattern=v{{major}}.{{minor}}
            type=semver,pattern=v{{major}}

#      - name: Buildx
#        uses: docker/setup-buildx-action@v3
#
#      - name: Build and push Docker image
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          push: true
#          cache-from: type=gha
#          cache-to: type=gha,mode=max
#          tags: ${{ steps.meta.outputs.tags }}


